{
  "mcpServers": {
    "fleet": {
      "command": "pnpm",
      "args": ["--dir", "{{FLEET_DIR}}", "--filter", "@fleet/mcp", "start"],
      "env": {
        "FLEET_PROJECT": "<project-name-from-fleet.config>",
        "FLEET_URL": "{{FLEET_URL}}"
      }
    }
  }
}
